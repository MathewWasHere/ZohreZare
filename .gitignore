# خروجی ساخت بسته‌ی استقرار (با tools/build-deploy.sh ساخته می‌شود)
dist/
*.zip

# استثنا: نسخه‌ی نهایی بسته که برای دانلود روی گیت‌هاب نگه داشته می‌شود
!deploy/zohrezare-cpanel.zip
!deploy/zz-api-patch.zip

# سیستم‌عامل / ویرایشگر
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# تنظیمات سرور — شامل رمز دیتابیس و کلید پیامک است
api/config.php

# وابستگی‌های ابزارها — با npm install --prefix tools بازسازی می‌شوند
tools/node_modules/
