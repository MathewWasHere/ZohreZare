/* ==========================================================================
   config.js — تنظیمات سراسری سایت
   همه‌ی مقادیر قابل‌تغییر (نام سالن، راه ارتباطی، رفتار OTP) اینجاست.
   ========================================================================== */
(function (global) {
  'use strict';

  var ZZ = (global.ZZ = global.ZZ || {});

  ZZ.config = {
    /* ---------- حالت نمایشی ----------
       true  → فقط برای توسعه/پیش‌نمایش: ورود با کد ثابت ۱۲۳۴،
               نمایش کد روی صفحه و «مدیرِ محلی» فعال است.
       false → پروداکشن: همه‌ی این‌ها خاموش است؛ ورود فقط از
               سرور واقعی (پیامک) ممکن می‌شود.
       ⚠️ بسته‌ی آپلود (tools/build-deploy.sh) این مقدار را
       خودکار به false قفل می‌کند؛ دستی برنگردانید. */
    demo: true,

    /* ---------- هویت برند ---------- */
    brand: {
      name: 'زهره زارع',
      latin: 'ZOHREH ZARE',
      /* زیرنویس لوگو در هدر و فوتر */
      sub: 'LIP & LASH & BROW',
      tagline: 'آکادمی تخصصی لب، مژه و ابرو',
      domain: 'zohrezare.ir',
      siteUrl: 'https://zohrezare.ir',
      /* شماره با نقطه‌ی کم‌رنگ بین بخش‌ها (کلاس .phone-dot در CSS) */
      phone: '۰۹۱۷۸۳۹۹۰۵۵',
      phoneHref: '+989178399055',
      address: 'شهرستان فسا، مصلی، روبه‌روی تأمین اجتماعی، جنب دفتر پیشخوان حسینی',
      addressShort: 'فسا، مصلی، روبه‌روی تأمین اجتماعی',
      /* مختصات: 28°56'52.7"N 53°38'08.0"E */
      geo: { lat: 28.947972, lng: 53.635556, zoom: 17 },
      instagram: 'https://www.instagram.com/zohrezaree.ir/',
      whatsapp: 'https://wa.me/989178399055',
      telegram: 'https://t.me/Zohrezare198260',
      /* زمان مراجعه ثابت نیست؛ همه‌ی خدمات فقط با تعیین وقت قبلی انجام می‌شوند. */
      hours: [
        { day: 'تمام روزهای هفته', time: 'فقط با تعیین وقت قبلی' }
      ]
    },

    /* ---------- نقشه ----------
       اگر کلید Google Maps دارید اینجا بگذارید تا نقشه‌ی تعاملی
       نمایش داده شود. بدون کلید، یک کارت نقشه‌ی ساده با لینک
       مستقیم به Google Maps نشان داده می‌شود (بدون هزینه و بدون
       نیاز به حساب). راهنما در README.
       دریافت کلید: console.cloud.google.com → Maps JavaScript API */
    maps: {
      apiKey: '',            // مثلاً 'AIzaSy...'
      mapId: ''              // اختیاری — برای استایل سفارشی
    },

    /* ---------- احراز هویت ---------- */
    auth: {
      /* نسخه‌ی نمایشی: کد ثابت همیشه معتبر است */
      mockMode: true,
      staticCode: '1234',
      codeLength: 4,
      resendSeconds: 60,
      maxAttempts: 5,
      /* در نسخه‌ی نمایشی کد روی صفحه نمایش داده می‌شود */
      revealCodeOnScreen: true
    },

    /* ---------- رزرو ---------- */
    booking: {
      daysAhead: 14,        // تعداد روزهایی که تقویم جلو می‌رود
      currency: 'تومان',
      cancelWindowHours: 24 // تا چند ساعت قبل امکان لغو هست
    },

    /* ---------- جریان تأیید رزرو ----------
       سایت بدون درگاه پرداخت کار می‌کند: مشتری ساعت را انتخاب
       می‌کند، رزرو به حالت «در انتظار تأیید» می‌رود، مدیر تماس
       می‌گیرد و بعد از هماهنگی (و در صورت لزوم دریافت بیعانه)
       تأییدش می‌کند. */
    approval: {
      /* خاموش کردن این گزینه یعنی برگشت به تأیید خودکار — مثلاً
         هر وقت درگاه پرداخت راه افتاد. */
      enabled: true,

      /* آیا ساعتِ «در انتظار تأیید» برای بقیه قفل شود؟
         روشن = کسی نمی‌تواند همان ساعت را بگیرد. */
      holdSlot: true,

      /* حداکثر درخواستِ باز برای هر شماره — جلوی بستن کل تقویم
         با درخواست‌های الکی را می‌گیرد. */
      maxOpenRequests: 2,

      /* چند ساعت طول می‌کشد تا تماس بگیریم — فقط برای پیام به مشتری */
      responseHours: 6
    },

    /* ---------- بیعانه ----------
       بدون درگاه پرداخت: مدیر حین تماس تصمیم می‌گیرد بیعانه لازم
       است یا نه، مبلغ را می‌گیرد (کارت‌به‌کارت یا نقدی) و در پنل
       ثبتش می‌کند. مشتری هم در حساب خودش می‌بیند. */
    deposit: {
      /* 'admin' = مدیر موقع تماس تصمیم می‌گیرد
         'all' = برای همه‌ی رزروها لازم است
         'threshold' = فقط وقتی مبلغ خدمت از aboveAmount بیشتر باشد */
      policy: 'admin',
      aboveAmount: 3000000,
      /* مبلغ پیشنهادی که در فرم پنل از قبل پر می‌شود */
      suggested: 500000,
      methods: [
        { id: 'card',   label: 'کارت به کارت' },
        { id: 'cash',   label: 'نقدی در محل' },
        { id: 'online', label: 'پرداخت آنلاین' }
      ]
    },

    /* ---------- مدیریت ----------
       ⚠️ فقط در حالت نمایشی (config.demo === true) معنا دارد:
       ورودِ محلی با این شماره‌ها نقش «مدیر» می‌گیرد.
       در پروداکشن نقش را فقط سرور تعیین می‌کند و این آرایه
       در بسته‌ی آپلود خالی می‌شود. */
    admin: {
      phones: ['09178399055'],
      panelPath: 'panel/admin/index.html'
    },

    /* ---------- اتصال به بک‌اند ----------
       خالی بگذارید تا خودکار از همان دامنه‌ی صفحه استفاده شود.
       این حالت برای پروداکشن درست است: فرانت و API روی یک دامنه‌اند
       و کوکی بدون دردسر رد و بدل می‌شود.

       فقط برای توسعه‌ی محلی که فرانت روی 8080 و API روی 8000 است،
       آدرس کامل بدهید:  baseUrl: 'http://localhost:8000'

       اگر بک‌اند بالا نباشد، سایت خودکار به حالت localStorage
       برمی‌گردد و همه‌چیز مثل قبل کار می‌کند. */
    api: {
      baseUrl: '',
      enabled: true
    },

    /* ---------- ذخیره‌سازی ---------- */
    storage: {
      prefix: 'zz.v1.'
    }
  };
})(window);
