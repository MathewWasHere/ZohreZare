<?php
/* ==========================================================================
   config.sample.php — نمونه‌ی تنظیمات سرور

   این فایل را روی هاست به config.php کپی کنید و مقادیر را پر کنید:
       cp api/config.sample.php api/config.php

   ⚠️ config.php هیچ‌وقت نباید در گیت ذخیره شود — کلید و رمزها داخلش است.
      در .gitignore هم گذاشته شده و .htaccess جلوی خوانده‌شدنش از وب را
      می‌گیرد.
   ========================================================================== */

return [

    /* ---------------- دیتابیس ----------------
       این مقادیر را از cPanel → MySQL Databases بردارید.
       نام دیتابیس و کاربر معمولاً با پیشوند حساب هاست می‌آید،
       مثلاً: zohreza_booking و zohreza_app */
    'db' => [
        'host'     => 'localhost',
        'name'     => '',
        'user'     => '',
        'pass'     => '',
        'charset'  => 'utf8mb4',
    ],

    /* ---------------- پیامک: نیازپرداز ----------------
       مستندات: https://niazpardaz-sms.com/webservice

       نکته‌ی مهم درباره‌ی «پترن»:
       نیازپرداز endpoint جداگانه‌ای برای پترن ندارد. متن کامل را با
       همان متد معمولی ارسال می‌کنیم و سامانه آن را با الگوی تأییدشده‌ی
       شما تطبیق می‌دهد. اگر متن با الگو نخواند، کد خطای ۱۸
       («مغایرت متن با قالب») برمی‌گردد. پس otp_template باید دقیقاً
       همان چیزی باشد که در پنل تأیید شده، با {code} به‌جای جای کد. */
    'sms' => [
        /* روشن/خاموش کردن ارسال واقعی. در حالت false هیچ پیامکی
           فرستاده نمی‌شود و کد فقط در لاگ می‌نشیند — برای تست. */
        'enabled'   => true,

        'provider'  => 'niazpardaz',

        /* نام کاربری پنل پیامکی */
        'username'  => '',

        /* کلید API — در پنل نیازپرداز ساخته می‌شود.
           اگر حساب شما با کلید کار نکرد، رمز عبور پنل را بگذارید و
           auth_mode را روی 'password' تنظیم کنید. */
        'api_key'   => '',

        /* 'apikey' = مقدار api_key به‌جای رمز عبور فرستاده می‌شود
           'password' = مقدار api_key همان رمز عبور پنل است
           هر دو حالت یک پارامتر می‌فرستند؛ این فقط برای وضوح است. */
        'auth_mode' => 'apikey',

        /* شماره‌ی خط فرستنده — از پنل، بخش خطوط من (مثلاً 10001234).
           برای پیامک کد تأیید باید خط خدماتی باشد. */
        'from'      => '',

        /* آدرس وب‌سرویس REST */
        'base_url'  => 'http://in.payamak-service.ir/api/v2/RestWebApi',

        /* ثانیه */
        'timeout'   => 15,

        /* --------- متن پیامک‌ها ---------
           {code} و {date} و {time} جایگزین می‌شوند.
           هر متنی که اینجا می‌گذارید باید در پنل به‌عنوان الگو
           تأیید شده باشد، وگرنه خطای ۱۸ می‌گیرید. */
        'templates' => [
            /* الگوی تأییدشده‌ی کد ورود */
            'otp'      => "کد تایید شما: {code}",

            /* این‌ها هنوز الگوی تأییدشده ندارند — تا وقتی تأیید نشوند
               خاموش بمانند (مقدار null یعنی پیامک فرستاده نمی‌شود). */
            'approved' => null,   // مثلاً: "نوبت شما در {date} ساعت {time} تایید شد."
            'rejected' => null,
            'reminder' => null,
        ],
    ],

    /* ---------------- کد یک‌بارمصرف ---------------- */
    'otp' => [
        'length'       => 4,
        'ttl_seconds'  => 120,   // اعتبار کد
        'resend_after' => 60,    // فاصله‌ی مجاز تا درخواست دوباره
        'max_attempts' => 5,     // تلاش اشتباه مجاز برای هر کد
        /* سقف درخواست کد از یک شماره در ساعت — جلوی خالی‌کردن
           اعتبار پیامکی توسط ربات‌ها را می‌گیرد */
        'per_hour'     => 5,
        /* سقف درخواست از یک IP در ساعت */
        'per_ip_hour'  => 20,
    ],

    /* ---------------- مدیر ----------------
       شماره‌هایی که بعد از ورود نقش «مدیر» می‌گیرند. */
    'admin_phones' => ['09178399055'],

    /* ---------------- عمومی ---------------- */
    'app' => [
        /* برای ساخت لینک در پیامک‌ها */
        'site_url' => 'https://zohrezare.ir',
        /* در حالت true جزئیات خطا برمی‌گردد — روی سرور واقعی false */
        'debug'    => false,
    ],
];
